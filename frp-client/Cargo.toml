[package]
name = "frp-client"
version = "0.69.1"
edition = "2021"
license = "MIT"

[dev-dependencies]
frp-server = { path = "../frp-server" }
tracing-subscriber.workspace = true
md-5.workspace = true

[dependencies]
frp-core = { path = "../frp-core", default-features = false }
frp-vnet = { workspace = true, optional = true }
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
anyhow.workspace = true
tracing.workspace = true
thiserror.workspace = true
uuid.workspace = true
axum = { workspace = true, optional = true }
toml.workspace = true
rustls = { workspace = true, optional = true }
tokio-rustls = { workspace = true, optional = true }
rand.workspace = true
data-encoding.workspace = true

[features]
default = ["tls", "quic", "kcp", "websocket", "oidc", "vnet", "admin", "compression", "chacha20", "tcp-mux"]
admin = ["dep:axum", "frp-core/admin-auth"]
tls = ["frp-core/tls", "dep:rustls", "dep:tokio-rustls"]
quic = ["frp-core/quic"]
kcp = ["frp-core/kcp"]
websocket = ["frp-core/websocket"]
oidc = ["frp-core/oidc"]
vnet = ["frp-core/vnet", "dep:frp-vnet"]
compression = ["frp-core/compression"]
chacha20 = ["frp-core/chacha20"]
tcp-mux = ["frp-core/tcp-mux"]
